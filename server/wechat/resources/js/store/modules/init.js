const state = {}

const getters = {}

const actions = {
    async init({dispatch}) {
    },
    async initWechatConfig () {
    }
}

const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}
